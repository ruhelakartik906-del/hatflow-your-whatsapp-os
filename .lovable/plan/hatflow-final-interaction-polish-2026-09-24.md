# Hatflow final interaction polish

## Scope
- Preserve every existing section, color, price, and content block.
- Reduce integration marks to a compact 32–40px range while preserving proportions and labels.
- Rebalance the desktop header so the six navigation links are mathematically centered between the brand and actions.
- Raise the hero dashboard slightly without changing the split layout.

## Navigation and CTAs
- Add stable section targets and shared sticky-header scroll offset so every navigation link lands with its heading visible.
- Correct Product, Solutions, Integrations, Automations, Pricing, Resources, CRM, AI, feature, and use-case actions.
- Add the requested section-level CTAs using the existing button styling.
- Keep Login as an external product action; route pricing and marketing actions to the appropriate form dialog.

## Forms
- Add separate accessible Get Started and Talk to an Expert dialogs.
- Include the exact requested fields and requirement options.
- Support close button, backdrop click, Escape, focus placement, scroll locking, and a clear successful submission state.
- Keep submission front-end only because no form delivery service is connected.

## Verification
- Test desktop and mobile layout, sticky-header offsets, modal behavior, every navbar item, all page CTAs, pricing actions, footer links, and horizontal overflow.
- Confirm no browser console errors.
